## What this changes
<!-- One sentence: what and why -->

## Checklist
- [ ] `ruff check . && ruff format .` passes (if touching Python)
- [ ] `uv run pytest tests/ -v` passes (if touching test config)
- [ ] README.md and README.pypi.md kept in sync (if touching docs)
- [ ] PR targets `development`, not `main`
